## 2024-05-24 - Double-Click Reset Pattern
**Learning:** Users expect "Double-click to reset" on range inputs (sliders/knobs) in audio/DJ interfaces.
**Action:** Implement `onDoubleClick` handlers on all range inputs to reset to default values (0 or center), and add `title="Double-click to reset"` for discoverability.
